import styled from 'styled-components/macro';

export const Container = styled.div`

    > select {
        padding: 10px 15px;
        border-radius: 5px;
        cursor: pointer;
        margin-left: 10px;
    }
`;